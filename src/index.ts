#!/usr/bin/env node

const command = process.argv[2];

if (command === 'serve') {
  console.error('my-pixso-mcp: server implementation is under construction (stage 1).');
  process.exit(1);
}

console.log('my-pixso-mcp: available commands: serve (under construction)');
